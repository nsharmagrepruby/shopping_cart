<h1>Product list</h1>
<table border="1">
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Price</th>
    <th>Quantity</th>
  </tr>
  <% @products.each do |product| %>
    <tr>
      <th><%= product.name %></th>
      <th><%= product.description %></th>
      <th><%= product.price %></th>
      <th><%= product.quantity %></th>
      <th>
        <%= form_with model: @cart_product, url: user_carts_path ,local: true do |form|  %>
        <%= form.label :quantity %><br>
        <%= form.number_field :quantity %>
        <%= form.hidden_field :products_id, value: product.id %>
        <%= form.submit "add to cart" %>
        <% end %>
      </th>
    </tr>
  <% end %>
</table>